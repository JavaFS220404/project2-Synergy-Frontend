export class Spell {

    constructor(public id: string, public name: string, public incantation: string,
        public effect: string, public canBeVerbal: boolean, public type: string, public light: string, public creator: string) { }
}
