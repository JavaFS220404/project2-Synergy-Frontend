export class Character {
    constructor(public id: string, public character: string, public nickname : string,
        public hogwartsStudent: boolean, public hogwartsHouse: string, public interpretedBy: string, public image: string) { }
}
